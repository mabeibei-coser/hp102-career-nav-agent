# Context Checkpoint — HP102 初始页面改为纯对话开场

存档时间：2026-09-25 12:05 +08:00；分支：main。
用户要求：写停止点，关机后再继续。已中断测试进程，确认 3102/3103 无监听；不要自动继续执行。

## 1. 任务目标

按用户确认的 A 方案：新对话只有欢迎语和输入框，允许就业相关自由问答；用户明确要求开始测评、填写档案或生成职业导航报告后才出档案卡。
授权仅本地代码与 mock 验证，不调用真实模型、不部署、不推送、不改其他项目；已有对话保留进度，重新测评仍回档案卡。
静态计划：`.planning/2026-09-25-chat-opening.md`；进度唯一主文件：`docs/plan/PROGRESS.md`。本 checkpoint 是本次暂停的最新状态，补充其末尾进度。

## 2. 进度

- 已实现：删除新建会话自动档案卡及 profile 阶段 fallback；替换欢迎语；同步提示词、工具说明、mock，利用当前任务完整卡片历史区分未开始与已出卡；普通自我介绍不触发建档；失败提示不再引用不存在的卡片。
- 已更新：需求、技术、执行计划与进度；单元/API/UI 测试，UI 填表/上传前显式开始；重测测试区分历史题卡、已验证登录卡与当前控件。
- 已验证：147 单测通过（最后一次在 12:02，之后仅追加 lib/errors.ts 文案和 http.test.ts 对应断言，尚未复跑）；typecheck 通过、lint 0 error/19 既有 unused 警告（在最后测试辅助函数修改前执行）；git diff --check 通过。
- E2E：首轮全量中止以修测试前置条件；第二轮选定 72 项，停止前输出到第 69 项。桌面/Pixel 的开场、多轮自由聊天、明确开始、完整报告、刷新、档案与简历均有通过证据；桌面重测已通过。iPhone 22 项因 WebKit 2359 缺失未启动，已官方安装且 launch OK，尚未补跑。第二轮两个重测登录断言仍用运行开始时的旧测试快照而失败，文件已修，待新进程复验；不能称整批通过。
- 未完成：最终 E2E 复跑、最终 typecheck/lint、生产构建、维护 close、本地提交。截图人工检查 1280×900/390×844 无初始卡片/无横向溢出，路径 `%TEMP%/codex-hp102-opening-review/{desktop,mobile}.png`，仅临时证据。

## 3. 关键决策（含 why）

- 不新增入口按钮、不改接口/数据库/阶段枚举，以最小调整保留现有完整流程；老用户已确认档案仅在主动出卡时预填。
- 提示词的 `profileCardShown` 从当前 task 的全部已存卡片计算，避免最近 40 条历史截断后把已开始的用户误当新用户；这不是新增数据库字段。
- 真实自然语言意图识别依靠模型提示词；mock 只能证明配置及流程，不代表真实 Gemini 行为已验收。
- 已修改的提示词文件是本项目 `lib/agent/skill/career-navigation.md`，并非共享 SKILL.md。未改共享规则/凭证/生产数据。

## 4. 失败尝试（不要再走的方向）

- 仅去掉创建卡片仍会由 fallback 自动补回，已同时取消 profile fallback；不要恢复旧的“只要说个人信息就建档”规则。
- 原测试 helper 假定进入即有上传控件，导致报告页面测试等待；`startAssessmentViaUi` 已接入确认档案与上传 helper，已有卡片则不重复开始。
- 重测后历史题卡仍在，按全页相同题号定位会 strict-mode 多匹配；已改为最新题号与可操作控件。旧登录卡标题仍在属于正常历史，ui-login.spec.ts 已改为两份报告摘要、只保留一张旧登录卡且无登录输入框。
- WebKit 旧版 2272 不匹配当前 Playwright，需要 2359；已 `PLAYWRIGHT_SKIP_BROWSER_GC=1 npx playwright install webkit` 官方下载并验证启动，未清理旧浏览器。无需重新下载。
- 不用 `git -c core.autocrlf=false diff --check`，会把正常 CRLF 误报为整文件变更/尾空白；用仓库原设置，`git -c core.safecrlf=false diff --check` 可减少警告。单测用 `--maxWorkers=2`，首次高并发下简历解析测试曾超过 5 秒。

## 5. 下一步具体行动

1. 用户恢复后读本 checkpoint、项目 AGENTS、上述 plan/PROGRESS，先核对 git diff，不覆盖其他任务变更；当前没有占用文件/仍运行的测试。
2. 运行下方命令复验 72 项（三端）；测试被中断未生成可靠 `.last-run.json`，不要只用 `--last-failed`。如要先排障，先跑 `ui-login.spec.ts` 的桌面/Pixel，再补 iPhone。测试仍使用配置中的 mock，3102/3103 必须没有未知服务，运行前核对 global-setup 的 e2e 测试路径范围。
3. E2E 停止并退出服务后再 `npm run build`，不要和 dev 共写 `.next`。生产构建不能设置 `E2E_MOCK_MODE=true`（启动检查会拒绝）；可用 false + 明确测试占位 BANANAROUTER_* 指向 127.0.0.1:9，数据库指定临时路径，禁止真实调用/输出密钥。最终复跑 unit/typecheck/lint。
4. 在 PROGRESS 记录真实结果与限制；维护文档登记本轮范围或无新增理由，再实际 check/close（下节）。check 曾列 `tests/e2e/ui-report-page.spec.ts` 为历史待审，但 git 干净且本轮未改它，不能为清零一并登记。不要运行会自动认领所有 pending 的 `scripts/maintenance-close-phase1.ts`。
5. Agent Handoff：上一手 Codex；下一手 Codex 可继续，因为需求和实现已确定；模型实现/本地验证绿灯，真实模型、部署、推送均不在授权内。本轮未 commit；验证/收尾后按项目要求只提交本轮文件，保留既有 `deploy/remote-init.sh`，不替用户删除临时或历史材料。

## 6. 相关文件 / 命令 / git 状态

未提交变更：`docs/plan/{01-需求方案,02-技术方案,03-执行计划,PROGRESS}.md`；`lib/agent/{prompt.ts,tools.ts,skill/career-navigation.md}`；`lib/career/copy.ts`；`lib/conversation/{service,view}.ts`；`lib/errors.ts`；`lib/llm/mock.ts`；`tests/e2e/{api-conversation,ui-chat-shell,ui-login,ui-profile-card}.spec.ts` 和 `ui-helpers.ts`；`tests/unit/{actions,agent-loop,agent-prompt,conversation-view,http}.test.ts`。
本轮新增：`.planning/2026-09-25-chat-opening.md`、本 checkpoint；既有未跟踪 `deploy/remote-init.sh` 不属于本任务，禁止提交/修改。源码改动全部已保存磁盘。
最近提交：`6814cd2 T2.1-T3.3: Phase 2+3 implementation`；`1ac0743 T1.16-T1.43: Gemini 冒烟与打字作答评测通过`；`6dc9e6d T0.1-T1.42: 第1期纯文字全流程（mock 模式）`。

```powershell
$env:PYTHONUTF8='1'; $env:PYTHONIOENCODING='utf-8'
$env:E2E_MOCK_MODE='true'
npx vitest run --maxWorkers=2
npx playwright test tests/e2e/ui-chat-shell.spec.ts tests/e2e/ui-profile-card.spec.ts tests/e2e/ui-login.spec.ts tests/e2e/ui-report-page.spec.ts tests/e2e/button-flow.spec.ts tests/e2e/typed-flow.spec.ts tests/e2e/refresh.spec.ts tests/e2e/ui-restart.spec.ts tests/e2e/api-conversation.spec.ts --project 'Desktop Chrome' --project 'Pixel 7' --project 'iPhone 14'
npm run typecheck
npm run lint
pwsh -NoProfile -File '..\.gstack\maintenance\Invoke-ExperienceCheck.ps1' check
pwsh -NoProfile -File '..\.gstack\maintenance\Invoke-ExperienceCheck.ps1' close --help
```

维护 close 必须明确本轮文件清单、实际验证/限制及逐文件预期 SHA256（含主经验文档和 .maintenance-experience.json）；不要认领其他 pending。当前只执行过 check（结构正常，待审 exit_code=2），尚未 close；暂停不是完成，不写完成结论。
